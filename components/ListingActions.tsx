"use client";
import {useState} from "react";import Link from "next/link";import {useRouter} from "next/navigation";import {createClient} from "@/lib/supabase/client";
export function ListingActions({id,published}:{id:string;published:boolean}){const [busy,setBusy]=useState(false);const router=useRouter();async function toggle(){setBusy(true);const s=createClient();await s.from("books").update({is_published:!published}).eq("id",id);router.refresh();setBusy(false)}async function remove(){if(!confirm("Eliminar este anúncio? Esta acção não pode ser anulada."))return;setBusy(true);const s=createClient();const {data:images}=await s.from("book_images").select("storage_path").eq("book_id",id);if(images?.length)await s.storage.from("book-images").remove(images.map(x=>x.storage_path));await s.from("books").delete().eq("id",id);router.refresh()}return <div className="listing-actions"><Link href={"/books/"+id+"/edit"}>Editar</Link><button onClick={toggle} disabled={busy}>{published?"Pausar":"Publicar"}</button><button className="danger" onClick={remove} disabled={busy}>Eliminar</button></div>}