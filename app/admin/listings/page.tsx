import Link from "next/link";import {redirect} from "next/navigation";import {AppShell} from "@/components/AppShell";import {createClient} from "@/lib/supabase/server";import {AdminListingAction} from "@/components/AdminListingAction";
export default async function AdminListings(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect("/auth");const {data:p}=await s.from("profiles").select("role").eq("id",user.id).single();if(!p||!["admin","moderator"].includes(p.role))redirect("/profile");const {data:books}=await s.from("books").select("id,title,price_kz,is_published,status,created_at,city,seller_id").order("created_at",{ascending:false}).limit(100);return <AppShell><section className="admin-page container"><Link className="back-link" href="/admin">← Administração</Link><h1>Moderar anúncios.</h1><div className="admin-table">{books?.map(b=><article key={b.id}><div><strong>{b.title}</strong><span>{Number(b.price_kz||0).toLocaleString("pt-AO")} Kz · {b.city||"Angola"} · {b.status}</span></div><AdminListingAction id={b.id} published={b.is_published}/></article>)}</div></section></AppShell>}