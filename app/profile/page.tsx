import Link from "next/link";import {AppShell} from "@/components/AppShell";import {createClient} from "@/lib/supabase/server";import {ProfileStats} from "@/components/ProfileStats";
export default async function ProfilePage(){ const locale:Locale="pt"; const t=messages[locale];const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return <AppShell><section className="profile-page container"><h1>{t.profile.loginTitle}</h1><Link className="button" href="/auth">{t.profile.login}</Link></section></AppShell>;const {data:profile}=await s.from("profiles").select("*").eq("id",user.id).single();const {count:books}=await s.from("books").select("id",{count:"exact",head:true}).eq("seller_id",user.id);const {data:reviews}=await s.from("seller_reviews").select("rating").eq("seller_id",user.id);const rating=reviews?.length?(reviews.reduce((a,r)=>a+r.rating,0)/reviews.length):0;return <AppShell><section className="profile-page container"><div className="profile-hero"><div className="profile-avatar-large">{profile?.avatar_url?<img src={profile.avatar_url} alt=""/>:(profile?.display_name||user.email||"U").slice(0,2).toUpperCase()}</div><div className="profile-hero-copy"><span className="eyebrow">{t.profile.account}</span><h1>{profile?.display_name||user.email?.split("@")[0]}</h1><p>{[profile?.city,profile?.municipality].filter(Boolean).join(", ")||"Angola"}{profile?.school?" · "+profile.school:""}</p>{profile?.bio&&<div className="profile-bio">{profile.bio}</div>}</div><Link className="secondary-button" href="/profile/edit">{t.profile.edit}</Link></div><ProfileStats books={books||0} reviews={reviews?.length||0} rating={rating}/><div className="profile-links-grid"><Link href="/sell">{t.profile.publish}</Link><Link href="/profile/listings">{t.profile.listings}</Link><Link href="/profile/favorites">{t.profile.favorites}</Link><Link href="/messages">{t.profile.messages}</Link><Link href="/notifications">{t.profile.notifications}</Link><Link href="/books">{t.profile.explore}</Link></div></section></AppShell>}